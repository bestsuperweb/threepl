//= require jquery.turbolinks//= require jquery_ujs//= require moment//= require bootstrap-datetimepicker//= require Chart.bundle//= require chartkick//= require_tree .// $(document).on ( 'turbolinks:load', function(){$(function() {	var preloader = $('#preloader');	$(window).on('load', function(){		preloader.delay(3000).fadeOut(500).remove();	});	if( $('#delivery-date').length ){		$('#delivery-date').datetimepicker({			format: 'YYYY-MM-DD HH:mm',			minDate: moment()		});	}		$('#send-email').click(function(event) {		/* Act on the event */		var _this = this;		$(_this).attr('disabled', true);		if( $('#products tbody').html() == '' ){			$("#alertModal").modal('show');			return;		}		var products = [];		var send_url = $(this).attr('data-url');		var delivery_date = $('#delivery-date input.form-control').val();		$('table#products tbody tr').each(function(index,element){			var image 	= $(element).children('td.image').children('img').attr('src');			var id 		= $(element).children('td.id').html();			var title	= $(element).children('td.title').children('b').html();			var weight 	= $(element).children('td.weight').html();			var width 	= $(element).children('td.width').html();			var length 	= $(element).children('td.length').html();			var height 	= $(element).children('td.height').html();			var battery = $(element).children('td.battery').html();			var shiping = $(element).children('td.shiping').html();			var order 	= $(element).children('td.order').html();			var notes 	= $(element).children('td.notes').html();			products.push({ image: image, title: title ,weight: weight, width: width, height: height, length: length, has_battery: battery, shipping_volume: shiping, order_quantity: order, notes: notes });		});		$.ajax({			url: send_url,			type: 'POST',			dataType: 'json',			data: { products: products, delivery_date: delivery_date },			success: function(data) {			      alert(data.status);			      $(_this).attr('disabled', false);			    }		});			});	$('body').on('click', '.product-box .round', function(event){		$(this).children('input').click();		if ( $(this).children('input').prop('checked') ){			$(this).parent('.product-box').addClass('selected');			$("#specModal").modal('show');		}else{			$(this).parent('.product-box').removeClass('selected');		}			});	$('body').on('click', '.edit-specs', function(event) {		event.preventDefault();		/* Act on the event */		var weight 	= $(this).parent().parent().children('td.weight').html();		var width  	= $(this).parent().parent().children('td.width').html();		var height 	= $(this).parent().parent().children('td.height').html();		var length 	= $(this).parent().parent().children('td.length').html();		var battery = $(this).parent().parent().children('td.battery').html();		var shiping = $(this).parent().parent().children('td.shiping').html();		var order 	= $(this).parent().parent().children('td.order').html();		var notes 	= $(this).parent().parent().children('td.notes').html();		$('#specModal #save-specs').attr('data-id', $(this).attr('data-id'));		$('#specModal #weight-input').val(weight);		$('#specModal #width-input').val(parseFloat(width));		$('#specModal #height-input').val(parseFloat(height));		$('#specModal #length-input').val(parseFloat(length));		$('#specModal #battery-input').val(battery);		$('#specModal #shiping-input').val(shiping);		$('#specModal #order-input').val(order);		$('#specModal #notes-input').val(notes);		$("#specModal").modal('show');	});	$('body').on('click', '.delete-specs', function(event) {		event.preventDefault();		/* Act on the event */		$(this).parent().parent().remove();	});	$('#specModal #save-specs').on('click', function(event) {		event.preventDefault();		/* Act on the event */				var weight 	= $('#specModal #weight-input').val();		var width 	= $('#specModal #width-input').val();		var height 	= $('#specModal #height-input').val();		var length 	= $('#specModal #length-input').val();		var battery = $('#specModal #battery-input').val();		var shiping = $('#specModal #shiping-input').val();		var order 	= $('#specModal #order-input').val();		var notes 	= $('#specModal #notes-input').val();		var id 		= $(this).attr('data-id');		$('tr#'+id+' td.weight').html(weight);		$('tr#'+id+' td.width').html(width);		$('tr#'+id+' td.height').html(height);		$('tr#'+id+' td.length').html(length);		$('tr#'+id+' td.battery').html(battery);		$('tr#'+id+' td.shiping').html(shiping);		$('tr#'+id+' td.order').html(order);		$('tr#'+id+' td.notes').html(notes);		$("#specModal").modal('hide');	});});