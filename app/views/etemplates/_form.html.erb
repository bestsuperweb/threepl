<%= form_for(@template) do |f| %>
  <% if @template.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@template.errors.count, "error") %> prohibited this template from being saved:</h4>

      <ul>
      <% @template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group required">
    <label class="control-label">Template ID</label><br>
    <%= f.text_field :template_id, class: 'form-control' %>
  </div>
  <div class="form-group required">
    <%= f.label :template_name, class: 'control-label' %><br>
    <%= f.text_field :template_name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit 'Submit', class: 'btn btn-success' %>
  </div>
<% end %>
